{
  "name": "internship-cup",
  "packageManager": "yarn@1.22.22",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "postinstall": "yarn workspaces backend prisma generate"
  },
  "devDependencies": {
    "turbo": "^2.5.2"
  }
}
